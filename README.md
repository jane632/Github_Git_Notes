# Github_Git_Notes
This is a repository that contains end to end notes on gitbash on Windows
## The following are the commands in github:
git add, git commit, git push, git merge, git branch, git checkout,git status, git stash, git log